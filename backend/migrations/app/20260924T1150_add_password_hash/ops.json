[
  {
    "id": "column.public.user.passwordHash",
    "label": "Add column \"passwordHash\" to \"user\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "passwordHash",
        "table": "user"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"passwordHash\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "user",
          "passwordHash"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"passwordHash\"",
        "sql": "ALTER TABLE \"public\".\"user\" ADD COLUMN \"passwordHash\" text"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"passwordHash\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "user",
          "passwordHash"
        ]
      }
    ]
  },
  {
    "id": "alterNullability.setNotNull.user.passwordHash",
    "label": "Set NOT NULL on \"user\".\"passwordHash\"",
    "operationClass": "destructive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "passwordHash",
        "table": "user"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"passwordHash\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "user",
          "passwordHash"
        ]
      },
      {
        "description": "ensure no NULL values in \"passwordHash\"",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"public\".\"user\" WHERE \"passwordHash\" IS NULL) AS \"result\"",
        "params": []
      }
    ],
    "execute": [
      {
        "description": "set NOT NULL on \"passwordHash\"",
        "sql": "ALTER TABLE \"public\".\"user\" ALTER COLUMN \"passwordHash\" SET NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"passwordHash\" is NOT NULL",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3 AND \"is_nullable\" = $4)) AS \"result\"",
        "params": [
          "public",
          "user",
          "passwordHash",
          "NO"
        ]
      }
    ]
  }
]